{
  "name": "advent-of-code-solutions",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt export && nuxt build",
    "build:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt build",
    "generate:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt build --target static && nuxt export",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.11.0",
    "node-sass": "^4.14.1",
    "nuxt": "^2.13.0",
    "nuxt-buefy": "^0.3.31",
    "sass-loader": "^8.0.2"
  },
  "devDependencies": {
    "push-dir": "^0.4.1",
    "node-sass": "^4.14.1",
    "sass-loader": "^8.0.2"
  }
}
